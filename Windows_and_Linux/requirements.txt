darkdetect
google-generativeai
openai
pyperclip
pynput
PySide6
markdown2
pyinstaller
ollama
psutil
requests

# Windows clipboard support (only on Windows)
pywin32; sys_platform == "win32"

# Qt complement
PySide6-stubs

# Linux clipboard tools for better image detection
# These are system packages that need to be installed via package manager
# Ubuntu/Debian: sudo apt-get install xclip xsel
# Fedora/RHEL: sudo dnf install xclip xsel
# Arch Linux: sudo pacman -S xclip xsel
xclip
xsel

